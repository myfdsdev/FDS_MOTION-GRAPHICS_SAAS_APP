# Set to "true" to use mock data (no backend needed)
# Set to "false" to call the real backend at /api/*
VITE_USE_MOCKS=

# Stripe publishable key for client-side checkout
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
